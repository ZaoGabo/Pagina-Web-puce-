# Configuracion de Base de Datos SQLite
# El archivo de base de datos se crea automaticamente
DATABASE_URL="file:./dev.db"

# Secreto para firmar tokens JWT (cambiar en produccion)
JWT_SECRET="zaoshop_jwt_secret_2024_cambiar_en_produccion"

# Tiempo de expiracion del token (ej: 24h, 7d)
JWT_EXPIRES_IN="24h"

# Origen permitido para CORS (URL del frontend)
CORS_ORIGIN="http://localhost:8080"

# Puerto del servidor
PORT=3000

# Entorno (development / production)
NODE_ENV="development"
